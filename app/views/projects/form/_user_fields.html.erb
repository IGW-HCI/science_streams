<div class="nested-fields row">
    <div class="col-xs-8">
        <%= f.input :email, readonly: f.object.persisted? %>
        <%= f.hidden_field :_destroy %>
        <%= f.hidden_field :id %>
    </div>

    <div class="col-xs-4">
        <%= link_to_remove_association "Remove User", f %>
    </div>
</div>
